<%= form_for(@pricing) do |f| %>
  <% if @pricing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pricing.errors.count, "error") %> prohibited this pricing from being saved:</h2>

      <ul>
      <% @pricing.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :per_gram %><br>
    <%= f.text_field :per_gram %>
  </div>
  <div class="field">
    <%= f.label :per_eighth %><br>
    <%= f.text_field :per_eighth %>
  </div>
  <div class="field">
    <%= f.label :per_quarter %><br>
    <%= f.text_field :per_quarter %>
  </div>
  <div class="field">
    <%= f.label :per_half %><br>
    <%= f.text_field :per_half %>
  </div>
  <div class="field">
    <%= f.label :per_ounce %><br>
    <%= f.text_field :per_ounce %>
  </div>
  <div class="field">
    <%= f.label :per_item %><br>
    <%= f.text_field :per_item %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
